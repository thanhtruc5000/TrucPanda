<link rel="stylesheet" href="./assets/stylesheets/index.css">

<h1><%= t ".title" %></h1>
    
    <%= link_to "Đăng nhập", new_session_path unless authenticated? %>
    <%= link_to "Thêm sản phẩm", new_product_path if authenticated? %>
    <div id="products">
      <% @products.each do |product| %>
        <div>
          <%= link_to product.name, product %>
        </div>
      <% end %>
    </div>

